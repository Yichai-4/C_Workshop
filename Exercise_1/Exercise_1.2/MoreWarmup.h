#pragma once

/** @file Warmup.h
 *  Warmup utilities
 */

 /** @brief checks if the user input isn't 'Q' or 'q'
  *
  * @param userInput the char entered by the user
  * @returns int value in terms of user's input
  */
int ValidInput(char userInput);